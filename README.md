# HTTP (Beginner) Server in C Project

With this project, I attempted to make a simple HTTP server which could handle GET requests in C. It is a single-threaded server, meaning only one client can access it at a time.